Choose one deterministic ROCm Doctor repair recipe.
Do not include shell commands.
Set config_patch.path to the active config filename.
Set config_patch.changes to an empty object unless the schema requires deterministic recipe paths.
The executor computes and validates the actual patch.
